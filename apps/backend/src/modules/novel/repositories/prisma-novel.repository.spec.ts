import { PrismaNovelRepository } from './prisma-novel.repository';

describe(PrismaNovelRepository.name, () => {
  // let uut: PrismaNovelRepository

  // beforeEach(async () => {})

  it.todo('write unit tests for PrismaNovelRepository');
});
