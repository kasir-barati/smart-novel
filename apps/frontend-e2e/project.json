{
  "name": "frontend-e2e",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "implicitDependencies": ["frontend"],
  "targets": {
    "e2e": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "node {projectRoot}/src/support/run-frontend-e2e.mjs {projectRoot}"
        ],
        "parallel": false
      }
    }
  }
}
